name: CI - DrMindSetFit

on:
  push:
    branches: [ main, feat/** ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies (clean)
        run: npm ci

      - name: Cache Playwright Browsers
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/ms-playwright
            ~/.cache/playwright
          key: ${{ runner.os }}-ms-playwright-${{ hashFiles('**/package-lock.json') }}-pw-1.58.1
          restore-keys: |
            ${{ runner.os }}-ms-playwright-

      - name: Install Playwright Browsers
        run: npx playwright install chromium --with-deps

      - name: Debug Playwright Install
        run: |
          node -v
          npm -v
          npx playwright --version
          ls -la ~/.cache/ms-playwright || true

      - name: Verify (tsc + lint + build)
        run: npm run verify


      - name: List Playwright Projects (CI guardrail)
        run: |
          npx playwright test --list || true
          echo CI checking chromium project
          npx playwright test --list | grep chromium >/dev/null

      - name: Run E2E Tests
        run: npm run test:e2e -- --project=chromium
